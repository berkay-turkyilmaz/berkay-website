import { AiLabChatPanel } from "@/features/ai-lab/components/ai-lab-chat-panel";

/** Prompt sandbox (PDF / deneme) — sohbet paneli, mod pathname’dan gelir. */
export default function AiLabSandboxPage() {
  return <AiLabChatPanel />;
}
